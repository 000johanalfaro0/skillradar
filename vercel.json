{
  "buildCommand": "corepack pnpm --filter @skillradar/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
